<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>@ViewBag.Title</title>

    <!-- Latest compiled and minified CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">

    <!-- Latest compiled JavaScript -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</head>
<body>
    <div>
        <link href="~/lib/bootstrap/css/bootstrap.css"rel="stylesheet" />
    </div>
    <div>
        <header>
            <img src="~/Images/banner_library.jpg"/>
        </header>
        <nav class="navbar navbar-expand-sm bg-dark-subtle">
            @Html.ActionLink("Home", "Index", null, new { @class = "btn btn-success" })
            @Html.ActionLink("Login", "Login", null, new { @class = "btn btn-success" })
            @Html.ActionLink("Books", "products", null, new { @class = "btn btn-success" })
            @Html.ActionLink("Buy", "Buy", null, new { @class = "btn btn-success" })
            @Html.ActionLink("Register", "register", null, new { @class = "btn btn-success" })
            @Html.ActionLink("Return", "returndate", null, new { @class = "btn btn-success" })
        </nav>
        @RenderBody()
        <footer>

        </footer>
    </div>
</body>
</html>
